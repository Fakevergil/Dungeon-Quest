#include<iostream>
#include<string>
#include "Skeleton.h"
using namespace std;

// Skeleton constructor initializes the enemy with predefined stats
Skeleton::Skeleton() : Enemy("Skeleton", 50, 50, 12, 6, 5, 1, 0, 35) {
	// No additional setup required
}